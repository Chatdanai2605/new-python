[project]
name = "new-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Chatdanai2605", email = "qqalm123@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "pandas>=3.0.6",
    "requests>=2.34.2",
]

[project.scripts]
new-python = "new_python:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
